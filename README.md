---

## 🚀 一鍵安裝

請使用 `root` 權限在終端執行以下命令：

```bash
curl -sSL https://raw.githubusercontent.com/YouKap/cwh/main/hwc.sh | sudo bash
```

首次執行會自動安裝腳本至 `/usr/local/bin/hwc` 並進入主選單。
之後僅需執行：

```bash
hwc
```

即可隨時啟動管理介面。

---
